import colors from './colors';

const theme = {
  colors: {
    primary: colors.blueViolet,
    secondary: colors.mintGreen,
    highlight: colors.goldenYellow,
    hover: colors.softMauve,
    background: colors.offWhite,
    textPrimary: colors.darkGray,
  },
  fonts: {
    primary: "'Inter', sans-serif",
    secondary: "'Playfair Display', serif",
  },
};

export default theme;
